---
title: C#的ref和out
toc: true
tag: C++

---

这两个都是引用，实现上都是是指针，区别在于语义。

**ref**是引用，你在函数内可能会获取值，可能会赋值，c#要求变量在使用前必须要初始化，ref修饰的变量在传参前就必须要先初始化。

**out**是输出，（变相的多返回值），为了解决早年c#没有元组的多返回值尴尬，它在语义上是离开函数时一定会被赋值，那么在传参之前变量不必初始化，在函数内一定要赋值，保证语义正确。