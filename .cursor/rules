# VVVVVenxo's Blog - 项目规范

这是一个基于 Hexo + Butterfly 主题的个人博客项目，部署在 GitHub Pages。

## 项目结构

```
VVVenoxBlog/
├── source/
│   ├── _posts/          # 文章目录
│   ├── img/             # 全局图片（banner、头像、封面）
│   │   └── covers/gen/  # 程序化生成的文章封面（勿手动改）
│   └── about/           # 关于页面
├── scripts/             # 封面生成脚本（见「文章封面」章节）
├── _config.yml          # Hexo 主配置
├── _config.butterfly.yml # Butterfly 主题配置
└── .github/workflows/   # GitHub Actions 自动部署
```

分支说明：源文件在 `source` 分支（默认分支，在此写作与提交）；`gh-pages` 是 Actions 构建产物，线上服务从它读取，勿手动改。

## 新建文章

```bash
hexo new "文章标题"
```

会自动创建：
- `source/_posts/文章标题.md` - 文章文件
- `source/_posts/文章标题/` - 同名文件夹，用于存放文章图片

## 图片使用

### 文章内图片
将图片放在文章同名文件夹中，使用相对路径引用：
```markdown
![描述](图片名.png)
```

### 全局图片
放在 `source/img/` 目录：
- `banner.jpg` - 首页大图
- `Avatar.jpg` - 头像
- `covers/` - 文章封面图

## 分类规范

| 一级分类 | 说明 | 示例 |
|----------|------|------|
| 技术笔记 | 技术学习内容 | C++、C#、Unity、网络、图形学 |
| 项目实战 | 个人项目、实习复盘 | Demo 开发记录、工作总结 |
| 随笔杂谈 | 非技术内容 | 方法论、读书笔记、生活感悟 |

二级分类通常是具体技术栈：`C++`、`Unity`、`网络` 等。

## Front-matter 模板

```yaml
---
title: 文章标题
date: 2025-01-01 12:00:00
toc: true
categories:
  - 技术笔记
  - C++
tags:
  - 关键词1
  - 关键词2
description: 50-100字的文章摘要，用于 SEO 和文章卡片展示
---
```

### 可选字段
- `sticky: 1` - 置顶文章
- `cover: /img/covers/xxx.jpg` - 自定义封面图。**通常不用写**——留空则由封面脚本按分类自动生成（见下）。只有当文章有真实关联图（如项目截图）时才手动指定。
- `updated: 2026-07-25` - 更新时间（重大修改时加）

## 文章封面（重要）

封面**程序化生成**，统一采用「三色极光霓虹 + 全息轨道 + 霓虹标题」骨架，并按分类切换中景元素与调色板；标题 hash 会稳定改变极光曲线、轨道角度和粒子位置，避免同分类撞图。写文章时 **front-matter 不用写 `cover:`**，交给脚本。

### 发布流程

写好 md 放 `source/_posts/` 后，仓库根目录执行：

```bash
bash scripts/new-cover.sh     # 给缺 cover 的文章生成封面（SVG + PNG），并写回 cover 字段
git add -A
git commit -m "新增文章：xxx"
git push origin source        # 推送后 Actions 自动构建部署
```

`new-cover.sh` 只处理没有 `cover:` 的文章，已有封面的会跳过（尊重手动指定，如项目实战用真实截图）。

### 其他用法

- 改配色/风格后全部重刷：`bash scripts/new-cover.sh --all`
- 分类三色调色板定义在 `scripts/gen_covers.py` 的 `PALETTES`，中景类型定义在 `VARIANTS`；新增分类时各加一行
- `--all` 只重绘自动生成的封面，永远保留手动指定的真实项目截图
- 依赖：Python 3 + 本机 Chrome/Chromium/Edge（脚本自动探测）
- 完整说明见 `scripts/README.md`

## 注意事项

1. **文件名避免特殊字符**：不要在文件名中使用 `#`（会导致 URL 解析问题）
2. **日期字段必填**：否则每次提交文章日期会变化
3. **标题使用 H2 起始**：文章内容从 `##` 开始，因为 `#` 是文章标题

## 部署

推送到 `source` 分支后，GitHub Actions 会自动构建并部署到 `gh-pages` 分支（线上服务源）。约 30 秒完成。

## 本地预览

```bash
hexo server
```

访问 http://localhost:4000
